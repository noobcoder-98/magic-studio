<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MagicStudio.UI.DualFFplayControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d">

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <canvas:CanvasControl
            x:Name="DualCanvas"
            Grid.Row="0"
            Draw="DualCanvas_Draw"
            CreateResources="DualCanvas_CreateResources" />

        <TextBlock
            Grid.Row="0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Margin="6,4"
            Text="DualFFplay — 1 CanvasControl, 2 players, shared D3D11 device"
            FontSize="11"
            Foreground="White"
            Opacity="0.7" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" Padding="8">
            <Button Content="Play"  Click="Play_Click"  />
            <Button Content="Pause" Click="Pause_Click" />

            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <TextBlock Text="Speed A" VerticalAlignment="Center" FontSize="12" />
                <NumberBox x:Name="SpeedANumberBox"
                           Minimum="0.1" Maximum="100.0"
                           Value="1.0"
                           SmallChange="0.1"
                           LargeChange="0.5"
                           SpinButtonPlacementMode="Hidden"
                           Width="90" />
                <Button Content="Set" Padding="10,4" Click="SetSpeedA_Click" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <TextBlock Text="Speed B" VerticalAlignment="Center" FontSize="12" />
                <NumberBox x:Name="SpeedBNumberBox"
                           Minimum="0.1" Maximum="100.0"
                           Value="1.0"
                           SmallChange="0.1"
                           LargeChange="0.5"
                           SpinButtonPlacementMode="Hidden"
                           Width="90" />
                <Button Content="Set" Padding="10,4" Click="SetSpeedB_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
