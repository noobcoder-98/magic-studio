<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MagicStudio.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MagicStudio.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MagicStudio">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="6" Padding="8,4"
                    Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Button Content="Open File"     Click="OpenFile_Click" />
            <Button Content="Dual: Open A"  Click="OpenDualA_Click" />
            <Button Content="Dual: Open B"  Click="OpenDualB_Click" />
        </StackPanel>

        <!-- Two examples stacked: top = single-video player, bottom = shared-GPU dual demo -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="4" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <local:MagicFFplayControl x:Name="FFplayPlayer" Grid.Row="0" />

            <Border Grid.Row="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

            <local:DualFFplayControl  x:Name="DualPlayer"   Grid.Row="2" />
        </Grid>
    </Grid>
</Window>
