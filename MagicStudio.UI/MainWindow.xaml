<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MagicStudio.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MagicStudio.UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MagicStudio">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="8,4"
                    Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Button Content="Open File" Click="OpenFile_Click" />
        </StackPanel>

        <!-- Side-by-side comparison: left = MagicFFmpegPlayer, right = MagicFFplay -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <!--<ColumnDefinition Width="*" />-->
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Existing player (MagicFFmpegPlayer.cpp) -->
            <!--<local:MediaPlayerControl x:Name="MediaPlayer" Grid.Column="0" />-->

            <!-- Splitter -->
            <Border Grid.Column="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

            <!-- New player (MagicFFplay.cpp ffplay.c C++ port) -->
            <local:MagicFFplayControl x:Name="FFplayPlayer" Grid.Column="2" />
        </Grid>
    </Grid>
</Window>
